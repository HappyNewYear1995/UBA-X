<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 1. 定义日志输出格式 -->
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- 2. 控制台输出 Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 3. 文件输出 Appender (按天滚动) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 基础日志文件路径 -->
        <file>logs/ubax-flink-job.log</file>
        <!-- 滚动策略：基于时间 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 历史日志文件命名格式 -->
            <fileNamePattern>logs/ubax-flink-job.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 保留 30 天的历史日志 -->
            <maxHistory>30</maxHistory>
            <!-- 日志文件总大小限制 -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 4. 屏蔽 Flink、Kafka 等底层框架的刷屏日志，只保留 WARN 及以上级别 -->
    <logger name="org.apache.flink" level="WARN" />
    <logger name="org.apache.kafka" level="WARN" />
    <logger name="org.apache.zookeeper" level="WARN" />
    <logger name="akka" level="WARN" />

    <!-- 5. 自己业务代码的日志级别 -->
    <logger name="com.huanniankj.module.flink" level="INFO" />

    <!-- 6. 根日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>